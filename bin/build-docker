#!/bin/bash
#
# Usage: ./bin/build-docker
#
# Builds the Docker docs image

if type docker >/dev/null ; then
    set -ex
    ./bin/build

    sudo docker build -t elusivebot-docs .
else
    echo "Docker not found!"
    exit 1
fi
